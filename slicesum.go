package go_helper

func SliceSumInt(slices ...[]int) (total int) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumUint(slices ...[]uint) (total uint) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumInt8(slices ...[]int8) (total int8) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumUint8(slices ...[]uint8) (total uint8) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumInt16(slices ...[]int16) (total int16) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumUint16(slices ...[]uint16) (total uint16) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumInt32(slices ...[]int32) (total int32) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumUint32(slices ...[]uint32) (total uint32) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumInt64(slices ...[]int64) (total int64) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumUint64(slices ...[]uint64) (total uint64) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumFloat32(slices ...[]float32) (total float32) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}

func SliceSumFloat64(slices ...[]float64) (total float64) {
	for _, slice := range slices {
		for _, element := range slice {
			total += element
		}
	}

	return
}
